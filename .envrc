# Env global (persiste entre workspaces e clones)
dotenv_if_exists $HOME/.config/open-class/api.env
# Arquivo local na raiz sobrescreve o global (permite customização por workspace)
dotenv_if_exists .env
