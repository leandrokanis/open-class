import { ProfilePageClient } from "@/components/profile/ProfilePageClient";

export const metadata = { title: "Configurações de perfil — Open Class" };

export default function ConfiguracoesPage() {
  return <ProfilePageClient />;
}
